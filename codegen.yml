overwrite: true
schema: "schema.graphql"
documents: "graphql/**/*.graphql"
generates:
  app/libs/generated/client.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
